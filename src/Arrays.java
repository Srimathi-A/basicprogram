
/*public class Arrays {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		int Upper=1;int Whitespace=1;int space=0;
		int Lower=1;int digit=0;int special=1;
		int vowel;int  consonant;
    String str = "Ac @";
    char[] ch=str.toCharArray();
    for(int i=1;i<ch.length;i++) {
    	System.out.println(ch[i]);
    }/*
    for(int i=1;i<ch.length;i++) {
    	 if(Character.isUpperCase(ch[i]) && Upper==0) {
    		System.out.println("Capital letter: "+ch[i]); 
    		Upper++;
    	 }
    		else if (Character.isLowerCase(ch[i])) {
    		System.out.println("small letter: "+ch[i]);
    	     Lower++;
    	 }
            else if (Character.isWhitespace(ch[i]) &&Whitespace==1) {
    	System.out.println("Whitespace : "+ch[i]);
    	Whitespace++;
    }
            else if (Character.isDigit(ch[i]) && digit==1) {
            	System.out.println("digit: "+ch[i+1]);
            	digit++;
            } 
            else if(ch[i]==' '&& space==0) {
            	System.out.println("space = "+ch[i]);
            }
    	// if(vowel==0 || consonant==0) {
    		// if(ch[i]=='a'||ch[i]=='e'||ch[i]=='i'||ch[i]=='o'||ch[i]=='u'
    			// ||ch[i]=='A'||ch[i]=='E'||ch[i]=='I'||ch[i]=='0'||ch[i]=='U') {
    			//	 System.out.println("vowel : "+(i+1));
    			//	 vowel++;
    		// }
    	// }
    				 
    			// else{
    				//ystem.out.println("consonant: "+(i+1));
    			//	 consonant++;
    			// }
    			 
    			// else if((ch[i]!=' '&& special ==0 && !Character.isLetter(ch[i]){
    			//	System.out.println("special: "+(i+1));
    			//	special++;
    			// }
    	// }*/
    //}
    	 
   

    
    
    	 	 
            

	


